# Download and install arm-gcc compiler and add to path. This doesn't
# run if already have it: i.e., if gcc-arm-none is already in your
# PATH after sys_deps.yml.

- name: Download GCC arm
  shell: wget https://launchpad.net/gcc-arm-embedded/5.0/5-2016-q1-update/+download/gcc-arm-none-eabi-5_3-2016q1-20160330-linux.tar.bz2

- name: unzip ARM
  shell: tar -jxvf gcc-arm-none-eabi-5_3-2016q1-20160330-linux.tar.bz2

- name: Create arm-gcc directory.
  shell: mkdir -p ~/gcc-arm-none-eabi

- name: Copy gcc-arm to destination directory.
  shell: cp -r ~/gcc-arm-none-eabi-5_3-2016q1/ ~/gcc-arm-none-eabi

- name: Check that bashrc exists.
  shell: touch ~/.bashrc

- name: Add arm-gcc directory to PATH
  shell: echo '\nexport PATH=$PATH:~/gcc-arm-none-eabi/gcc-arm-none-eabi-5_3-2016q1/bin' >> ~/.bashrc

- name: Update PATH
  shell: . ~/.bashrc
